{
  "name": "gateway",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/gateway/src",
  "tags": ["gateway"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node ../../node_modules/.bin/ts-node src/main.ts",
        "cwd": "apps/gateway"
      }
    }
  }
}
